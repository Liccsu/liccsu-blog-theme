---
description: Sky Blog Theme 项目开发规则和规范
globs:
alwaysApply: true
---
# Sky Blog Theme - 项目规则

## 📋 项目概述

**Sky Blog Theme** 是基于现代前端技术栈的 Halo 2.0 主题，集成 Vite、Tailwind CSS v4、DaisyUI 和 Alpine.js，提供优雅的博客体验。

### 主题特色
- **现代化设计**: 基于 Tailwind CSS v4 和 DaisyUI 的现代化 UI 设计
- **响应式布局**: 完美适配桌面端和移动端
- **主题切换**: 支持深色/浅色模式切换
- **性能优化**: 基于 Vite 的快速构建和热更新
- **类型安全**: 完整的 TypeScript 支持
- **组件化**: 基于 Alpine.js 的轻量级交互组件

## 🛠️ 环境要求

### 必需环境
- **Java**: 21+ (Halo 运行环境)
- **Node.js**: 20+ (前端开发环境)
- **npm**: 10+ (包管理器)
- **Halo**: 2.21+ (博客系统)

### 开发工具
- **IDE**: 推荐使用 VS Code 或 WebStorm
- **Git**: 版本控制
- **终端**: 支持现代 shell 环境 (推荐 Zsh)

## 🏗️ 技术栈规范

### 核心技术栈
- **构建工具**: Vite 7.0.4+ (快速构建和热更新)
- **类型系统**: TypeScript 5.8.3+ (类型安全开发)
- **前端框架**: Alpine.js 3.14.9+ (轻量级响应式框架)
- **样式框架**: Tailwind CSS 4.1.11+ (原子化CSS框架)
- **UI组件库**: DaisyUI 5.0.46+ (基于Tailwind的组件库)
- **CSS处理器**: PostCSS 8.5.6+ (CSS后处理器)
- **图标系统**: @iconify/tailwind4 1.0.6+ (图标集成)
- **后端框架**: Halo 2.21.3+ (博客系统)
- **模板引擎**: Thymeleaf 3.0.12+ (服务端模板)

### 开发工具链
- **代码检查**: ESLint 9.30.1+ (代码质量检查)
- **代码格式化**: Prettier 3.6.2+ (代码格式化)
- **Git钩子**: Husky 9.1.7+ (Git提交钩子)
- **预提交检查**: lint-staged 16.1.2+ (暂存文件检查)
- **主题打包**: @halo-dev/theme-package-cli 1.0.0+ (Halo主题打包)
- **包管理器**: npm 10+ (标准包管理)
- **构建系统**: Gradle (Java项目构建)

## 📁 项目结构规范

### 优化后的目录结构
```
theme-sky-blog-1/                # 主题根目录
├── src/                        # 前端源码目录
│   ├── common/                 # 公共资源目录
│   │   ├── main.js             # 公共资源入口文件
│   │   ├── css/                # 公共样式目录
│   │   │   ├── tailwind.css    # Tailwind CSS 框架
│   │   │   ├── main.css        # 主题核心样式
│   │   │   ├── nav-enhancements.css # 导航增强样式
│   │   │   └── base.css        # 基础公共样式
│   │   └── js/                 # 公共脚本目录
│   │       ├── alpine-modules.js # Alpine.js 公共模块
│   │       └── base.js         # 基础工具函数
│   ├── pages/                  # 页面特定资源目录
│   │   ├── index/              # 首页资源
│   │   │   ├── index.css       # 首页特定样式
│   │   │   └── index.js        # 首页特定脚本
│   │   └── post/               # 文章页资源
│   │       ├── post.css        # 文章页特定样式
│   │       └── post.js         # 文章页特定脚本
│   └── vite-env.d.ts           # TypeScript 声明文件
├── templates/                  # Halo 主题模板目录
│   ├── assets/                 # 构建后静态资源（自动生成）
│   │   ├── css/                # 构建后的CSS文件
│   │   │   ├── main.css        # 公共样式（271KB）
│   │   │   ├── index.css       # 首页样式（0.74KB）
│   │   │   └── post.css        # 文章页样式（2.52KB）
│   │   └── js/                 # 构建后的JS文件
│   │       ├── main.js         # 公共脚本（46KB）
│   │       ├── index.js        # 首页脚本（0.05KB）
│   │       └── post.js         # 文章页脚本（3.96KB）
│   ├── modules/                # 模板模块
│   │   ├── index/              # 首页相关模块
│   │   │   ├── header.html     # 首页头部
│   │   │   └── layout.html     # 首页布局
│   │   ├── footer.html         # 页脚模块
│   │   └── nav.html            # 导航栏模块
│   ├── index.html              # 首页模板
│   └── post.html               # 文章页模板
├── .codebuddy/                 # CodeBuddy 配置目录
│   └── .rules/                 # 项目规则目录
├── build.gradle                # Gradle 构建配置
├── vite.config.ts              # Vite 构建配置
├── theme.yaml                  # 主题配置
├── settings.yaml               # 设置配置
├── package.json                # 项目依赖
├── tsconfig.json               # TypeScript 配置
├── eslint.config.js            # ESLint 配置
├── prettier.config.js          # Prettier 配置
└── postcss.config.cjs          # PostCSS 配置
```

### 文件组织原则
- **公共资源统一管理**: `src/common/` 目录包含所有公共资源
- **页面资源独立存放**: `src/pages/` 目录按页面组织特定资源
- **构建产物自动生成**: `templates/assets/` 目录由构建系统自动生成
- **模块化设计**: 每个功能模块职责单一，便于维护

### 文件命名规范
- **目录名称**: 使用小写字母，如 `common/`, `pages/`
- **CSS文件**: 使用小写字母和连字符，如 `nav-enhancements.css`
- **JavaScript文件**: 使用小写字母和连字符，如 `alpine-modules.js`
- **模板文件**: 使用小写字母和连字符，如 `post-list.html`

## 🔧 构建系统规范

### Vite 构建配置
- **入口点自动扫描**: 自动识别 `src/pages/` 下的页面资源
- **公共资源合并**: `src/common/` 下的资源合并为 `main.js/css`
- **页面资源独立**: 每个页面的资源独立构建
- **热更新支持**: 开发时支持文件变化自动刷新

### 构建产物说明
```
构建流程:
src/common/main.js → templates/assets/js/main.js (46KB)
src/common/css/* → templates/assets/css/main.css (271KB)
src/pages/index/* → templates/assets/{js,css}/index.{js,css}
src/pages/post/* → templates/assets/{js,css}/post.{js,css}
```

### 新增页面流程
1. 在 `src/pages/` 下创建页面目录
2. 创建对应的 `.js` 和 `.css` 文件
3. 在 JS 文件中导入 CSS: `import './页面名.css';`
4. 运行 `npm run build` 自动构建

## 🎨 Alpine.js 组件规范

### 组件定义位置
- **公共组件**: `src/common/js/alpine-modules.js`
- **页面特定组件**: `src/pages/*/页面名.js`

### 当前公共组件
```javascript
// 模板中实际使用的组件
- themeToggle: 主题切换组件 (templates/modules/nav.html)
- backToTop: 回到顶部组件 (templates/modules/footer.html)
- headerController: 首页头部控制器 (templates/modules/index/header.html)
```

### 组件开发原则
- **按需开发**: 只实现模板中实际使用的功能
- **功能单一**: 每个组件职责明确
- **轻量化**: 避免过度设计和复杂功能

## 🖌️ 代码风格规范

### JavaScript/TypeScript 规范
- 使用 ESLint 和 Prettier 进行代码检查和格式化
- 使用 ES6+ 语法特性，如箭头函数、解构赋值等
- 函数和方法应添加 JSDoc 注释说明用途和参数
- 使用 `kebab-case` 命名文件，如 `alpine-modules.js`

```javascript
/**
 * 主题切换控制器
 * 模板使用：templates/modules/nav.html
 */
function createThemeToggle() {
  return {
    init() {
      // 初始化逻辑
    },
    
    toggleTheme() {
      // 切换主题逻辑
    }
  };
}
```

### HTML/Thymeleaf 规范
- 使用语义化标签，如 `<header>`, `<footer>`, `<main>`, `<article>` 等
- Thymeleaf 属性使用连字符命名法，如 `th:fragment="post-content"`
- 模板片段使用有意义的名称，如 `~{modules/post/content :: post-content}`
- 保持缩进一致，推荐使用 2 空格缩进

### CSS/Tailwind 规范
- 优先使用 Tailwind 原子类，减少自定义 CSS
- 自定义样式使用 BEM 命名规范
- 公共样式放在 `src/common/css/` 目录
- 页面特定样式放在对应的页面目录

## 🔄 开发流程规范

### 版本控制
- 使用 Git 进行版本控制
- 遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范
- 主要分支: `main` (稳定版本), `develop` (开发版本)

### 提交规范
```
<类型>(<范围>): <描述>

类型包括:
- feat: 新功能
- fix: 缺陷修复
- docs: 文档更新
- style: 代码风格调整
- refactor: 代码重构
- perf: 性能优化
- build: 构建系统相关
- chore: 其他修改
```

### 开发命令
- `npm run dev`: 启动开发服务器，监视文件变化
- `npm run build`: 构建主题包
- `npm run lint`: 运行代码检查
- `npm run prettier`: 格式化代码

## 🧪 功能检测规范

### 模板功能使用检测
使用以下命令检测模板中实际使用的功能：

```bash
# 搜索 Alpine.js 组件使用
grep -r "x-data\|@click\|Alpine" templates/

# 搜索特定组件
grep -r "themeToggle\|backToTop\|headerController" templates/

# 搜索全局函数
grep -r "toggleTheme\|scrollToTop" templates/
```

### 功能开发原则
1. **先检测再开发**: 确认模板中实际使用的功能
2. **最小化实现**: 只实现必要的功能
3. **定期清理**: 移除未使用的代码

## 🚀 部署规范

### 构建流程
1. 运行 `npm run build` 生成主题包
2. 构建产物位于 `dist/theme-sky-blog-1-*.zip`
3. 将主题包上传到 Halo 后台安装

### 性能指标
- **构建时间**: ~600-700ms
- **主题包大小**: ~0.08MB
- **JavaScript 总大小**: ~50KB (压缩后)
- **CSS 总大小**: ~280KB (包含 Tailwind)

## 📚 文档规范

### 代码注释
- 使用 JSDoc 为函数和类添加注释
- 标注模板使用位置，如 `// 模板使用：templates/modules/nav.html`
- 复杂逻辑添加行内注释说明

### 项目文档
- README.md: 项目概述、安装说明、使用方法
- .codebuddy/.rules/my-rule.mdc: 项目开发规则（本文档）
- 构建配置文档: vite.config.ts 中的注释说明

## 🎯 性能优化标准

### 加载性能目标
- 首屏加载时间 < 2 秒
- 完全加载时间 < 3 秒
- JavaScript 执行时间 < 100ms
- CSS 解析时间 < 50ms

### 优化策略
- **代码分割**: 公共资源与页面资源分离
- **按需加载**: 只加载当前页面需要的资源
- **资源压缩**: 自动压缩 JavaScript 和 CSS
- **缓存策略**: 合理设置静态资源缓存

## 🔍 故障排查指南

### 常见问题
1. **构建失败**: 检查 Node.js 版本和依赖安装
2. **样式不生效**: 确认 CSS 文件已在对应 JS 中导入
3. **组件不工作**: 检查 Alpine.js 组件是否正确注册
4. **模板报错**: 确认模板中使用的组件已在 `alpine-modules.js` 中定义

### 调试方法
- 使用浏览器开发者工具检查控制台错误
- 检查网络面板确认资源加载状态
- 使用 `console.log` 调试 Alpine.js 组件状态
- 查看构建日志确认构建过程是否正常